{
  "name": "@pluto/extension-ts",
  "private": true,
  "version": "0.1.0",
  "description": "TypeScript Pluto browser extension",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "dev": "pnpm run watch",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
