{
  "name": "pluto-frontend",
  "version": "0.1.0",
  "description": "Pluto Chrome extension frontend",
  "scripts": {
    "build": "tsc && node scripts/copy-assets.js",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
